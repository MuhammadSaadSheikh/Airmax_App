'use client';import{useState}from'react';import{useRouter}from'next/navigation';export default function Login(){const router=useRouter();const[identifier,setIdentifier]=useState('admin@airmax.pk');const[password,setPassword]=useState('');const[error,setError]=useState('');async function submit(e:React.FormEvent){e.preventDefault();const r=await fetch('/api/session',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({identifier,password})});if(!r.ok)return setError('Invalid administrator credentials');router.replace('/')}return <main className="grid min-h-screen place-items-center p-5"><form onSubmit={submit} className="panel w-full max-w-md p-8"><p className="text-sm font-black tracking-[.3em] text-[#00c8ff]">AIRMAX</p><h1 className="mt-5 text-3xl font-black">Admin sign in</h1><p className="muted mt-2 text-sm">Authorized operations personnel only.</p><label className="mt-8 block text-sm font-bold">Email or phone<input className="mt-2 w-full rounded-xl border border-[#20365e] bg-[#050b1e] p-4" value={identifier} onChange={e=>setIdentifier(e.target.value)}/></label><label className="mt-4 block text-sm font-bold">Password<input type="password" className="mt-2 w-full rounded-xl border border-[#20365e] bg-[#050b1e] p-4" value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<p className="mt-3 text-sm text-rose-400">{error}</p>}<button className="mt-6 w-full rounded-xl bg-[#00c8ff] p-4 font-black text-[#050b1e]">Sign in</button></form></main>}
