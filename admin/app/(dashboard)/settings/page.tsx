export default function Page(){return <><h1 className="text-3xl font-black">Settings</h1><p className="muted mt-2">Secure backend integrations and access control.</p><div className="mt-8 grid gap-4 md:grid-cols-2">{[['MikroTik','Configure encrypted router API credentials on the backend.'],['Roles & permissions','Manage admin scopes and audit requirements.'],['Notifications','Configure FCM, APNs, email and SMS providers.'],['Billing providers','Manage signed callback credentials and settlement rules.']].map(([a,b])=><section className="panel p-6" key={a}><h2 className="font-bold">{a}</h2><p className="muted mt-2 text-sm">{b}</p><button className="mt-5 text-sm font-bold text-[#00c8ff]">Configure →</button></section>)}</div></>}
