import { Text } from '@/components/AppText';
import Ionicons from '@react-native-vector-icons/ionicons'; import { useQuery } from '@tanstack/react-query'; import { router } from '@/navigation/router'; import { useMemo, useState } from 'react'; import { Pressable, StyleSheet, TextInput, View } from 'react-native'; import { Badge, Card, ErrorState, Header, LoadingState, Screen, ui } from '@/components/ui'; import { colors } from '@/constants/theme'; import { api } from '@/services/api';
export default function Customers(){const [search,setSearch]=useState('');const q=useQuery({queryKey:['customers'],queryFn:api.customers});const data=useMemo(()=>q.data?.filter(c=>`${c.name} ${c.phone} ${c.connectionId}`.toLowerCase().includes(search.toLowerCase())),[q.data,search]);return <Screen><Header title="Customers" subtitle="2,847 customer accounts" action={<Pressable onPress={()=>router.push('/admin/customer-form')} style={styles.add}><Ionicons name="add" color={colors.background} size={25}/></Pressable>}/><View style={styles.search}><Ionicons name="search" size={20} color={colors.muted}/><TextInput value={search} onChangeText={setSearch} placeholder="Search name, phone or ID" placeholderTextColor="#62779C" style={styles.input}/><Ionicons name="options-outline" size={20} color={colors.primary}/></View>{q.isLoading?<LoadingState/>:q.isError?<ErrorState retry={()=>q.refetch()}/>:data?.map((c,i)=><Pressable key={c.id} onPress={()=>router.push({pathname:'/admin/customer-detail',params:{id:c.id}})}><Card style={styles.card}><View style={styles.avatar}><Text style={styles.initial}>{c.name[0]}</Text></View><View style={{flex:1}}><View style={styles.row}><Text style={styles.name}>{c.name}</Text><Badge label={i===2?'pending':'active'} tone={i===2?'warning':'success'}/></View><Text style={ui.small}>{c.connectionId} · {c.phone}</Text><Text style={[ui.small,{marginTop:4}]}>{c.address}</Text></View><Ionicons name="chevron-forward" color={colors.muted} size={18}/></Card></Pressable>)}</Screen>}
const styles=StyleSheet.create({add:{width:42,height:42,borderRadius:13,backgroundColor:colors.primary,alignItems:'center',justifyContent:'center'},search:{height:52,borderRadius:15,backgroundColor:colors.surface,borderWidth:1,borderColor:colors.border,flexDirection:'row',alignItems:'center',paddingHorizontal:14,gap:10,marginBottom:15},input:{flex:1,color:colors.text},card:{marginBottom:10,flexDirection:'row',alignItems:'center',gap:12},avatar:{width:46,height:46,borderRadius:15,backgroundColor:'#0A3155',alignItems:'center',justifyContent:'center'},initial:{color:colors.primary,fontSize:18,fontWeight:'900'},row:{flexDirection:'row',justifyContent:'space-between',alignItems:'center'},name:{color:colors.text,fontSize:15,fontWeight:'800'}});
