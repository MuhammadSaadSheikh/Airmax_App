import { Text } from '@/components/AppText';
import Ionicons from '@react-native-vector-icons/ionicons'; import { useMutation } from '@tanstack/react-query'; import { router } from '@/navigation/router'; import { useState } from 'react'; import { Alert, Pressable, StyleSheet, View } from 'react-native'; import { Button, Card, Header, Screen, ui } from '@/components/ui'; import { api } from '@/services/api'; import { colors, money } from '@/constants/theme';
const methods=[['card-outline','Card payment'],['phone-portrait-outline','JazzCash'],['wallet-outline','Easypaisa'],['business-outline','Bank transfer']] as const;
export default function Payment(){const [method,setMethod]=useState('Card payment');const m=useMutation({mutationFn:()=>api.payBill('b3',method),onSuccess:r=>Alert.alert('Payment successful',`Receipt ${r.receipt}`,[{text:'Done',onPress:()=>router.replace('/(customer)/billing')}])});return <Screen><Header title="Pay bill" subtitle="Secure payment powered by AIRMAX"/><Card><Text style={ui.small}>AMOUNT DUE</Text><Text style={styles.amount}>{money(3500)}</Text><View style={ui.divider}/><View style={styles.line}><Text style={ui.body}>August 2026</Text><Text style={styles.bold}>AMX-2608-1042</Text></View></Card><Text style={ui.sectionTitle}>Payment method</Text>{methods.map(([icon,name])=><Pressable key={name} onPress={()=>setMethod(name)} style={[styles.method,method===name&&styles.selected]}><Ionicons name={icon} size={23} color={method===name?colors.primary:colors.muted}/><Text style={styles.methodText}>{name}</Text><Ionicons name={method===name?'radio-button-on':'radio-button-off'} size={21} color={method===name?colors.primary:colors.muted}/></Pressable>)}<Text style={[ui.body,{marginTop:18}]}>By continuing, you agree to the payment provider’s terms. Your card details are never stored by AIRMAX.</Text><Button title={`Pay ${money(3500)}`} icon="lock-closed-outline" loading={m.isPending} onPress={()=>m.mutate()}/></Screen>}
const styles=StyleSheet.create({amount:{fontSize:36,color:colors.text,fontWeight:'900',marginTop:8},line:{flexDirection:'row',justifyContent:'space-between'},bold:{color:colors.text,fontWeight:'700'},method:{height:64,borderRadius:16,borderWidth:1,borderColor:colors.border,backgroundColor:colors.surface,flexDirection:'row',alignItems:'center',padding:16,gap:13,marginBottom:10},selected:{borderColor:colors.primary,backgroundColor:'#08213E'},methodText:{color:colors.text,flex:1,fontWeight:'700'}});
