import { Text } from '@/components/AppText';
import { router } from '@/navigation/router'; import { useState } from 'react'; import { Button, Header, Input, Screen, ui } from '@/components/ui';
export default function Forgot(){const [email,setEmail]=useState('');const [sent,setSent]=useState(false);return <Screen><Header title="Reset password" subtitle="We’ll send a secure reset link"/>{sent?<><Text style={[ui.body,{marginBottom:16}]}>A reset link was sent to {email}. Check your inbox and spam folder.</Text><Button title="Back to sign in" onPress={()=>router.back()}/></>:<><Input label="Email address" icon="mail-outline" autoCapitalize="none" keyboardType="email-address" value={email} onChangeText={setEmail} placeholder="you@example.com"/><Button title="Send reset link" onPress={()=>setSent(true)} disabled={!email.includes('@')}/></>}</Screen>}
