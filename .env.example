# Mobile environment selection is owned by Android product flavors and iOS
# build configuration. This root file intentionally defines no runtime values.
#
# Backend configuration: server/.env.example
# Next.js admin configuration: admin/.env.example
