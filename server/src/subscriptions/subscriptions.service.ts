import{ConflictException,Injectable}from'@nestjs/common';import{SubscriptionStatus}from'@prisma/client';import{PrismaService}from'../prisma/prisma.service';@Injectable()export class SubscriptionsService{constructor(private prisma:PrismaService){}async create(userId:string,packageId:string){const active=await this.prisma.subscription.findFirst({where:{userId,status:{in:[SubscriptionStatus.ACTIVE,SubscriptionStatus.PENDING]}}});if(active)throw new ConflictException('An active subscription already exists');const plan=await this.prisma.package.findUniqueOrThrow({where:{id:packageId}});const startsAt=new Date();const expiresAt=new Date(startsAt.getTime()+plan.durationDays*86_400_000);return this.prisma.subscription.create({data:{userId,packageId,startsAt,expiresAt}})}list(userId:string){return this.prisma.subscription.findMany({where:{userId},include:{package:true},orderBy:{createdAt:'desc'}})}}
